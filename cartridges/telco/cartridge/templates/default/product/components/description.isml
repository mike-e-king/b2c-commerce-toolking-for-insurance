<isif condition="${product.shortDescription}">
    <div class="col-sm-12 col-md-12 col-lg-12 value content" id="collapsible-description-${loopState && loopState.count ? loopState.count : '1'}">
        <isprint value="${product.shortDescription}" encoding="off" />
    </div>
</isif>

