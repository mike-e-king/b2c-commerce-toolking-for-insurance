<isif condition="${product.longDescription}">
    <div class="col-sm-12 col-md-12 col-lg-12 value content" id="collapsible-details-${loopState && loopState.count ? loopState.count : '1'}">
        <isprint value="${product.longDescription}" encoding="off" />
    </div>
</isif>
